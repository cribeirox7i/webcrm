# Lido automaticamente pelo Vite em "npm run build" (modo production).
# Trocar pela URL pública real do backend (HTTPS) antes do primeiro deploy.
VITE_API_URL=https://SUBSTITUIR-PELA-URL-REAL-DO-BACKEND
