# Lido automaticamente pelo Vite em "npm run build" (modo production).
# Backend na VM webcrm-backend (IP estático 34.138.41.74), atrás do Caddy (proxy + TLS
# automático via Let's Encrypt), exposto via nip.io -- trocar por um domínio próprio
# quando um estiver disponível (nip.io é solução temporária, ver leva de deploy no STATUS.md).
VITE_API_URL=https://34.138.41.74.nip.io
